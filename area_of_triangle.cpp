#include<iostream>
using namespace std;
int main() 
{
  int height, base;
   cout<<"enter height and base";
   cin>>height>>base;
   int area;
       area = 0.5*height*base;
       cout<<"area of triangle is "<<area;
       return 0;
}